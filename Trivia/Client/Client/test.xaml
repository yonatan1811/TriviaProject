<Window x:Class="Client.test"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Client"
        mc:Ignorable="d"
        Title="test" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Room Name:" Grid.Row="0" Grid.Column="0" Margin="10"/>
        <TextBox x:Name="RoomNameTextBox" Grid.Row="0" Grid.Column="1" Margin="10"/>

        <TextBlock Text="Capacity:" Grid.Row="1" Grid.Column="0" Margin="10"/>
        <TextBox x:Name="CapacityTextBox" Grid.Row="1" Grid.Column="1" Margin="10"/>

        <Button Content="Create" Grid.Row="2" Grid.ColumnSpan="2" Margin="10" Click="CreateButton_Click"/>
        <Button Content="Cancel" Grid.Row="3" Grid.ColumnSpan="2" Margin="10" Click="CancelButton_Click"/>
    </Grid>
</Window>
